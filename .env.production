# The deployed edge proxies /api; no backend origin is compiled into JavaScript.
VITE_DATA_SOURCE=remote
# Used only by `vite preview`; production Nginx/Netlify own their proxy rules.
VITE_API_PROXY_TARGET=https://starforge.78.111.91.113.nip.io
