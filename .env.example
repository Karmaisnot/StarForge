# Production browser requests are proxied to the staff API to avoid CORS.
VITE_DATA_SOURCE=remote
VITE_API_PROXY_TARGET=https://starforge.78.111.91.113.nip.io
VITE_AUTH_ENDPOINT=role-login

# Isolated local development only. Never use these values in a deployment.
# VITE_DATA_SOURCE=local
# VITE_API_PROXY_TARGET=http://127.0.0.1:4000

# Explicit visual-only mode. Never use in a deployed environment.
# VITE_DATA_SOURCE=mock
